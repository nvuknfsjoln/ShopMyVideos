<!DOCTYPE html>
<html lang="de">
<head>
  <%- include('../partials/header') %>
  <title>Support Nachrichten</title>
</head>
<body>
  <main>
    <h1>Support-Anfragen</h1>
    <% if (messages.length > 0) { %>
      <ul>
        <% messages.forEach(msg => { %>
          <li>
            <strong><%= msg.email %></strong>: <%= msg.message %>
            <form action="/admin/support/delete/<%= msg._id %>" method="POST" style="display:inline;">
              <button type="submit">Löschen</button>
            </form>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p>Keine Support-Anfragen vorhanden.</p>
    <% } %>
  </main>
</body>
</html>
