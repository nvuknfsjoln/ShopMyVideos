<!DOCTYPE html>
<html lang="de">
<head>
  <%- include('../partials/header') %>
  <title>Alle Videos</title>
</head>
<body>
  <main>
    <h1>Alle verfügbaren Videos</h1>
    <div class="video-grid">
      <% videos.forEach(video => { %>
        <div class="video-card">
          <img src="<%= video.thumbnail %>" alt="Thumbnail" />
          <h3><%= video.title %></h3>
          <p><%= video.price %> €</p>
        </div>
      <% }) %>
    </div>
  </main>
</body>
</html>
